/**
 * @deprecated — Import from '@/app/utils/api-proxy' instead.
 * Thin re-export kept for backward compatibility during PRD-00 migration.
 */
export { BACKEND_URL } from './api-proxy';

/** @deprecated Use BACKEND_URL from api-proxy instead. */
export { BACKEND_URL as BFF_URL } from './api-proxy';
