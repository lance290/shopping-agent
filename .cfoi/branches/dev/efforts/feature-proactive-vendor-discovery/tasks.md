# Tasks — feature-proactive-vendor-discovery

1. Add candidate persistence models, migrations, and exports.
2. Implement classifier, coverage scorer, query planner, and dedupe foundation.
3. Implement discovery adapters, normalization, and orchestrator.
4. Integrate sync and streaming row search through the new vendor discovery seam.
5. Add targeted tests, run verification, and update build-all artifacts.
